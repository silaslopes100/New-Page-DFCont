# Mantis Security Review Packet — DFCont (New Page DFCont)

> This report was automatically generated by Mantis AI. All findings and patches are AI-generated and must be manually verified by a security or subject matter expert before deployment or disclosure.

**Target Version:** Git (remote: https://github.com/silaslopes100/New-Page-DFCont.git) — branch/commit not recorded in pass state (scan target: `D:\New Page DFCont`, commit `feb5207d` per VCS history).

**Snapshot ID:** (legacy — snapshot not recorded)

**Pass:** 1 · **Mode:** MODE-OFF (unpinned; findings scored and patched against the live working tree)

## Executive Summary

| Finding | Priority | Risk Score | Status |
|---|---|---|---|
| Backend fails to boot: self-import of PlanDB in plan.py causes ImportError | **CRITICAL** | 8.0 | Patch verified (VERIFIED_SECURE) |
| Contact router imports undefined ContactRequest/ContactResponse classes | **CRITICAL** | 8.0 | Patch verified (VERIFIED_SECURE) |
| CWE-287: unauthenticated, unlimited lead/contact writes (no auth, no rate limiting, no CSRF) | **HIGH** | 7.0 | Patch verified (VERIFIED_SECURE) |
| requirements.txt pins pydantic-email-validation==0.2.0 which does not exist on PyPI | **HIGH** | 6.4 | Patch verified (VERIFIED_SECURE) |
| CWE-209: raw exception text leaked to unauthenticated clients via detail=str(e) | **HIGH** | 6.0 | Patch verified (VERIFIED_SECURE) |
| DEBUG defaults to True: Swagger UI (/docs) exposed in production by default | **MEDIUM** | 3.5 | Patch verified (VERIFIED_SECURE) |
| Exploit Chain: Total service outage via supply-chain install failure + startup import regressions | **MEDIUM** | 5.6 | Exploit chain (static) |
| Exploit Chain: Unauthenticated error-oracle (CWE-209) + unauthenticated PII writes (CWE-287) - recon to DB abuse | **MEDIUM** | 4.8 | Exploit chain (static) |

The backend is **currently non-functional**: two independent import regressions (commit `feb5207d`) prevent it from booting, and the pinned dependency set cannot be installed in a clean environment. All findings below were empirically reproduced (or statically confirmed for the two chains) and every proposed patch was verified in an isolated shadow tree.

## Category 1 — Patch Independently Verified (VERIFIED_SECURE)

#### Backend fails to boot: self-import of PlanDB in plan.py causes ImportError

| Field | Value |
|---|---|
| **Finding ID** | `2b227e4c-7da7-40ec-bda7-8eb204014999` |
| **CWE** | CWE-248 |
| **Final Risk Score** | 8.0 / 10 |
| **Priority** | **CRITICAL** |
| **Inferred Exposure** | EXPOSED |
| **Attacker Position** | EXTERNAL |
| **Privileges Required** | NONE |
| **User Interaction** | NONE |
| **Reproduction** | reproduced |
| **Patch Status** | VERIFIED_SECURE |
| **Discovery Snapshot** | (legacy — not recorded) |

**Description.** backend/app/api/models/plan.py:6 executes `from app.api.models.plan import PlanDB` from within plan.py itself, before the PlanDB class (line 9) is defined. Python's import machinery reuses the partially initialized module from sys.modules; getattr for PlanDB fails. Verified on the live tree: `import app.api.routes.plans` raises `ImportError: cannot import name 'PlanDB' from partially initialized module 'app.api.models.plan'`. main.py imports app.api.routes.plans unconditionally (main.py:3), so the entire FastAPI application - every router, /health, startup DB init - fails to start. Introduced ...

**Impact.** Total service availability loss: backend cannot boot or be deployed; all API endpoints unreachable.

**Validation reasoning.** Verified empirically: `python -c "import app.api.routes.plans"` in backend/ raises ImportError: cannot import name 'PlanDB' from partially initialized module 'app.api.models.plan'. Line 6 exists as claimed; class defined at line 9. main.py:3 imports the router unconditionally so the app cannot boot. Direct, unambiguous, 100% reproducible. Rule 10: refined code_paths to the flawed line only.

**Production viability.** Self-import defect is present in the production code path with no debug gating; the app fails to boot in any deployment (verified import raises). Not debug-only, not assertion-dependent.

**Reproduction evidence.**

```
PoC script : D:\New Page DFCont\workspace\reproducers\poc_F1.py
Run command: python workspace/reproducers/poc_F1.py  (cwd = private shadow copy of backend)
Evidence base: UNPINNED-MODE-OFF
```

Excerpt of reproduction output (redacted):

```
ImportError: cannot import name 'PlanDB' from partially initialized module 'app.api.models.plan' (app/api/models/plan.py:6). Traceback names the sink file:line (channel-b evidence).
```

**Risk rationale.** Impact 4, likelihood 4 (exposure multiplier applied); Total backend outage on every deploy; marketing site backend dead; embarrassing and urgent for any operator, but no attacker actively exploits it - it is a defect, not an intrusion.

**Executive summary.** Self-import of PlanDB in plan.py:6 blocks the backend from booting at all. Deterministic, reproduced, unauthenticated-relevant (the outage exists before any request). Availability tier CRITICAL per threat model, no automatic recovery -> CRITICAL (8.0). Fix: delete line 6, add CI boot smoke test.

**Remediation.** Delete line 6 (`from app.api.models.plan import PlanDB`) from plan.py; the name is available in the same module. Add a startup smoke test (`import app.api.routes.plans` / uvicorn boot check) to CI.

**Verified patch.** plan.py self-import removed; plans/plan imports OK; variants blocked — Apply against: (not recorded)

---

#### Contact router imports undefined ContactRequest/ContactResponse classes

| Field | Value |
|---|---|
| **Finding ID** | `70221333-ed67-427d-bbc5-1446b95d0c84` |
| **CWE** | CWE-248 |
| **Final Risk Score** | 8.0 / 10 |
| **Priority** | **CRITICAL** |
| **Inferred Exposure** | EXPOSED |
| **Attacker Position** | EXTERNAL |
| **Privileges Required** | NONE |
| **User Interaction** | NONE |
| **Reproduction** | reproduced |
| **Patch Status** | VERIFIED_SECURE |
| **Discovery Snapshot** | (legacy — not recorded) |

**Description.** backend/app/api/routes/contact.py:2 executes `from app.api.models.plan import ContactRequest, ContactResponse`, but no module in the repository defines classes with those names (verified by repo-wide grep). This raises ImportError as soon as the contact router is imported (main.py:23), a second independent startup-breaking defect alongside the plan.py self-import. Additionally, models/contact.py defines ContactDB which no route ever uses; /api/contact/send, even if importable, is a stub returning a success message without persisting anything - it accepts arbitrary user input and reports succes...

**Impact.** Backend startup failure (with the plan.py fix it would still crash); contact form silently loses user submissions (data-integrity of the contact channel).

**Validation reasoning.** Repo-wide grep for 'class ContactRequest' / 'class ContactResponse' returns zero definitions; contact.py:2 imports them from models.plan (which defines only PlanDB/PlanResponse). Import of the router deterministically raises ImportError, blocking main.py:23 mounting. Secondary claim (ContactDB dead code, stub endpoint) also verified: models/contact.py:7 is referenced by no route. Rule 10: dropped ...

**Production viability.** Undefined class imports in production router code; import failure is unconditional in release builds. Not a test/mock-only path.

**Reproduction evidence.**

```
PoC script : D:\New Page DFCont\workspace\reproducers\poc_F2.py
Run command: python workspace/reproducers/poc_F2.py  (cwd = shadow with plan.py:6 removed to isolate)
Evidence base: UNPINNED-MODE-OFF
```

Excerpt of reproduction output (redacted):

```
ImportError: cannot import name 'ContactRequest' from 'app.api.models.plan' (routes/contact.py:2). Traceback names the sink file:line (channel-b evidence).
```

**Risk rationale.** Impact 4, likelihood 4 (exposure multiplier applied); Second independent boot-blocker; even after fixing plan.py the service still cannot start. Operational trust in the repo is already damaged by commit feb5207d.

**Executive summary.** contact.py:2 imports ContactRequest/ContactResponse that do not exist anywhere in the repo, crashing the app at import time (main.py:23). Deterministic and reproduced. Availability tier CRITICAL, no auto-recovery -> CRITICAL (8.0). Fix: define the models in models/contact.py or remove the stub.

**Remediation.** Define ContactRequest/ContactResponse (e.g., in models/contact.py alongside ContactDB) and either implement real persistence for /api/contact/send or remove the stub endpoint.

**Verified patch.** ContactRequest/ContactResponse defined in models/contact.py; route import redirected — Apply against: (not recorded)

---

#### CWE-287: unauthenticated, unlimited lead/contact writes (no auth, no rate limiting, no CSRF)

| Field | Value |
|---|---|
| **Finding ID** | `0cbda088-0dd1-4c78-a902-efa7e271672c` |
| **CWE** | CWE-287 |
| **Final Risk Score** | 7.0 / 10 |
| **Priority** | **HIGH** |
| **Inferred Exposure** | EXPOSED |
| **Attacker Position** | EXTERNAL |
| **Privileges Required** | NONE |
| **User Interaction** | NONE |
| **Reproduction** | reproduced |
| **Patch Status** | VERIFIED_SECURE |
| **Discovery Snapshot** | (legacy — not recorded) |

**Description.** POST /api/leads/create (lead.py:10) accepts arbitrary JSON and persists a LeadDB row with zero authentication, no rate limiting, no CSRF protection, and no origin checks beyond CORS (which is browser-enforced and does not stop curl). Pydantic bounds (name 2-100, email format, phone 10-20) are trivially satisfied by an attacker. Impact: unlimited spam rows fill the SQLite DB (disk exhaustion over time), pollute PII tables used for sales follow-up, and the response 400/500 behavior can be probed freely as an oracle. The same applies to the calculator endpoint (stateless, lower impact) and the co...

**Impact.** DB abuse/spam (lead table fill, PII pollution, disk growth), abusable error oracle; no integrity or privacy protection on PII ingestion.

**Validation reasoning.** Verified: lead.py:10 has no auth dependency, no rate limiting, no CSRF; service inserts rows per request. Threat model is Intent: PRODUCTION (dfcont.com.br), so this is a public internet-facing write endpoint - not rule-2 hygiene (that rule targets local-only/test functions). Triggerable by any client via curl; impact is DB fill/spam and PII pollution.

**Production viability.** Public production-intent endpoint (dfcont.com.br) with no auth/rate limiting; fully triggerable from any network client in a standard deployment.

**Reproduction evidence.**

```
PoC script : D:\New Page DFCont\workspace\reproducers\poc_F4.py
Run command: python poc_F4.py (TestClient against shadow with startup DB init)
Evidence base: UNPINNED-MODE-OFF
```

Excerpt of reproduction output (redacted):

```
Three POST /api/leads/create requests with NO auth headers returned HTTP 200 {"id":N,"message":"Lead cadastrado com sucesso!"} and persisted rows (ids 1-3).
```

**Risk rationale.** Impact 3, likelihood 4 (exposure multiplier applied); Anonymous junk flooding a real company's PII intake (sales follow-up) is an integrity annoyance and a GDPR/PI-data quality liability; publicly trivially demonstrable.

**Executive summary.** POST /api/leads/create accepts unlimited anonymous writes (no auth, no rate limit, no CSRF); reproduced: 3 unauthenticated POSTs persisted rows. Impact 3 (PII table pollution + disk growth), likelihood 4 -> 7.0 HIGH.

**Remediation.** Add authentication/authorization, per-IP and per-session rate limiting (e.g., slowapi or a proxy WAF), CSRF protection for state-changing endpoints, and input fingerprinting (honeypots, captcha).

**Verified patch.** per-IP in-memory 10/min rate limit wired into lead+contact; 12/25 bursts see 429; benign 3 requests 200 OK — Apply against: (not recorded)

---

#### requirements.txt pins pydantic-email-validation==0.2.0 which does not exist on PyPI

| Field | Value |
|---|---|
| **Finding ID** | `abd5f395-a5e3-42b9-a8fa-94a94fa63cee` |
| **CWE** | CWE-1104 |
| **Final Risk Score** | 6.4 / 10 |
| **Priority** | **HIGH** |
| **Inferred Exposure** | INTERNAL |
| **Attacker Position** | SUPPLY_CHAIN |
| **Privileges Required** | NONE |
| **User Interaction** | NONE |
| **Reproduction** | reproduced |
| **Patch Status** | VERIFIED_SECURE |
| **Discovery Snapshot** | (legacy — not recorded) |

**Description.** backend/requirements.txt lists pydantic-email-validation==0.2.0. Verified against PyPI: no distribution with that name exists (pip index returns 'No matching distribution found'). The intended package for Pydantic v2 EmailStr (used by models/lead.py:1,24) is `email-validator`. Consequences: (1) a fresh `pip install -r requirements.txt` fails during dependency resolution, blocking deployment (availability); (2) if a malicious actor ever publishes a package with that name, an automated install could pull it - a typo-squat/dependency-confusion risk; (3) email-validator itself is NOT declared, so ...

**Impact.** Deployment failure in clean environments; supply-chain exposure to package-name squatting; undeclared runtime dependency.

**Validation reasoning.** Verified against PyPI: 'pip index versions pydantic-email-validation' -> No matching distribution found. The declared package does not exist, so `pip install -r requirements.txt` fails in a clean environment (deployment availability) and the name is exposed to typo-squatting (supply chain). models/lead.py:1 imports EmailStr which needs email-validator - undeclared in requirements.txt. Rule 12: pat...

**Production viability.** Nonexistent pinned package breaks installs in every fresh environment; not config-dependent. (Squatting arm is conditional on attacker publishing; install failure is not.)

**Reproduction evidence.**

```
PoC script : D:\New Page DFCont\workspace\reproducers\F7_pip.txt
Run command: pip download --no-deps --dest pip_check pydantic-email-validation==0.2.0
Evidence base: UNPINNED-MODE-OFF
```

Excerpt of reproduction output (redacted):

```
ERROR: Could not find a version that satisfies the requirement pydantic-email-validation==0.2.0 (from versions: none); ERROR: No matching distribution found for pydantic-email-validation==0.2.0. (PyPI registry response = target-produced evidence.)
```

**Risk rationale.** Impact 4, likelihood 4 (exposure multiplier applied); A nonexistent pinned dependency is a deploy-blocker AND a standing invitation for package-squatting on PyPI; supply-chain-aware reviewers will react strongly.

**Executive summary.** requirements.txt pins pydantic-email-validation==0.2.0 which does not exist on PyPI (verified: 'from versions: none'); fresh installs fail (availability, CRITICAL tier) and name-squatting is possible. Supply-chain position multiplier 0.8 -> (4+4)*0.8 = 6.4 HIGH.

**Remediation.** Replace `pydantic-email-validation==0.2.0` with `email-validator>=2.0` (plus a pinned exact version); verify the full requirements set installs cleanly in an empty venv; consider hashing/pinning and a lockfile.

**Verified patch.** requirements pins email-validator==2.0.0; pip dry-run resolves; EmailStr imports — Apply against: (not recorded)

---

#### CWE-209: raw exception text leaked to unauthenticated clients via detail=str(e)

| Field | Value |
|---|---|
| **Finding ID** | `d072da85-80ee-4e8b-a44a-efef216d3b9d` |
| **CWE** | CWE-209 |
| **Final Risk Score** | 6.0 / 10 |
| **Priority** | **HIGH** |
| **Inferred Exposure** | EXPOSED |
| **Attacker Position** | EXTERNAL |
| **Privileges Required** | NONE |
| **User Interaction** | NONE |
| **Reproduction** | reproduced |
| **Patch Status** | VERIFIED_SECURE |
| **Discovery Snapshot** | (legacy — not recorded) |

**Description.** Routes wrap business logic in `except Exception as e: raise HTTPException(status_code=400, detail=str(e))` at lead.py:16, calculator.py:14 and contact.py:12. Any unexpected exception - SQLAlchemy/DB constraint violations, Pydantic coercion errors surfaced from deeper layers, path/OS errors - is echoed verbatim to the client, including module paths, file paths, SQL snippet fragments and library internals. Frontend amplifies the exposure: services/api.js:14 extracts `error.response.data.detail` into the thrown Error message, and Hero/CTA/Contact/Calculator forms render that message into the DOM....

**Impact.** Information disclosure of internal error details (paths, framework internals, DB errors) to unauthenticated remote clients; facilitates targeted follow-up attacks.

**Validation reasoning.** The pattern is confirmed in code: lead.py:16, calculator.py:14, contact.py:12 all do detail=str(e), and frontend/src/services/api.js:14 forwards detail into rendered Error messages. Attacker control (rule 13) holds for the ingress (unauthenticated POST bodies). However, triggerability depends on inducing a server-side exception (Pydantic 422s do NOT reach the handler; DB/constraint errors require ...

**Production viability.** Pattern is compiled into every release build (no debug gating). Endpoints are production-reachable and unauthenticated; disclosure triggers whenever any server-side exception occurs (DB/OS level). Repro will confirm exact trigger; viability of the flaw pattern itself is unconditional.

**Reproduction evidence.**

```
PoC script : D:\New Page DFCont\workspace\reproducers\poc_F3_F6.py
Run command: python poc_F3_F6.py (TestClient against shadow with plan.py:6 + contact stubs fixed)
Evidence base: UNPINNED-MODE-OFF
```

Excerpt of reproduction output (redacted):

```
HTTP 400 with {"detail":"SECRET_INTERNAL:/etc/dfcont/config/secrets.db INTERNAL_DETAIL_MARKER"} from a throwing service (sink confirmed). STRONGER: an unauthenticated real trigger leaked the full SQLAlchemy error: "(sqlite3.OperationalError) no such table: leads [SQL: INSERT INTO leads ...] [parameters: (...)]" to the client via detail=str(e).
```

**Risk rationale.** Impact 2, likelihood 4 (exposure multiplier applied); Raw SQL + bound parameters echoed to anonymous callers; even a hobbyist site looks amateur when error pages leak internals; low public outrage but real recon value.

**Executive summary.** detail=str(e) in 3 routes returns full exception text (SQL, parameters, paths) to unauthenticated clients; reproduced with a live SQLAlchemy error. Impact 2 (info leak), likelihood 4 (deterministic) -> 6.0 HIGH.

**Remediation.** Replace blanket `except Exception` + str(e) with validation-aware handling and a centralized error mapping that returns generic messages and logs full details server-side only; cap/neutralize the frontend interceptor to never surface raw detail strings.

**Verified patch.** generic error message in lead/calculator/contact routes; KeyError/RuntimeError/sqlite3 variants blocked; benign 200 OK — Apply against: (not recorded)

---

#### DEBUG defaults to True: Swagger UI (/docs) exposed in production by default

| Field | Value |
|---|---|
| **Finding ID** | `64f00b37-7afa-4eb7-a214-ce0ff0c010bb` |
| **CWE** | CWE-200 |
| **Final Risk Score** | 3.5 / 10 |
| **Priority** | **MEDIUM** |
| **Inferred Exposure** | EXPOSED |
| **Attacker Position** | EXTERNAL |
| **Privileges Required** | NONE |
| **User Interaction** | NONE |
| **Reproduction** | reproduced |
| **Patch Status** | VERIFIED_SECURE |
| **Discovery Snapshot** | (legacy — not recorded) |

**Description.** config.py:10 sets DEBUG from env with default 'True' (`os.getenv('DEBUG', 'True').lower() == 'true'`), and main.py:10 exposes docs_url='/docs' whenever DEBUG is true. No .env or deployment descriptor exists in the repo, so a default deployment serves the interactive OpenAPI/Swagger UI to the internet, advertising the full API schema, parameter models and validation rules - a free recon/intel asset for attackers. Additionally, /health (main.py:32-34) discloses app name/version.

**Impact.** Unauthenticated disclosure of the complete API surface and schema (OpenAPI), easing targeted attacks; minor version disclosure via /health.

**Validation reasoning.** config.py:10 verified: DEBUG = os.getenv('DEBUG', 'True').lower() == 'true' - defaults True; main.py:10 exposes docs_url when DEBUG. No .env/deployment descriptor in repo. Default deployment therefore serves /docs publicly. Factual information-exposure finding (CWE-200); LOW severity.

**Production viability.** Docs exposure depends on DEBUG env: default is True and no deployment descriptor sets it, so a default production deployment exposes /docs; an operator setting DEBUG=false blocks it.

**Reproduction evidence.**

```
PoC script : D:\New Page DFCont\workspace\reproducers\poc_F3_F6.py
Run command: python poc_F3_F6.py (TestClient, no env vars set)
Evidence base: UNPINNED-MODE-OFF
```

Excerpt of reproduction output (redacted):

```
GET /docs -> HTTP 200 text/html with swagger-ui present (DEBUG defaulted True). GET /health -> HTTP 200 {"status":"ok","app":"DFCont API","version":"1.0.0"}.
```

**Risk rationale.** Impact 2, likelihood 3 (exposure multiplier applied); Exposing the full OpenAPI schema to the internet by default is a classic amateur deployment tell; credibility hit for a professional services firm.

**Executive summary.** DEBUG defaults to True, so /docs + /openapi.json serve the full API schema anonymously (reproduced). Conditional viability: only exploitable under the default env (no .env in repo) -> 0.7 multiplier. Impact 2, likelihood 3 -> 3.5 MEDIUM. Fix: default DEBUG=False.

**Remediation.** Default DEBUG to False (fail-safe), require an explicit env var to enable; gate /docs behind DEBUG in production; consider hiding /health internals.

**Verified patch.** DEBUG defaults False; /docs,/openapi.json,/redoc all gated behind DEBUG (openapi_url/redoc_url added to the gate); /health OK; DEBUG=true env opt-in serves all three (verified on live tree) — Apply against: (not recorded)

---

## Exploit Chains (Not End-to-End Reproduced)

Chains are statically confirmed compositions of individually reproduced findings; the full end-to-end sequence was not re-run.

### Exploit Chain: Total service outage via supply-chain install failure + startup import regressions

| Field | Value |
|---|---|
| **Finding ID** | `30f7ca2e-0956-473a-aab6-99ecfce7eaa1` |
| **Priority / Score** | **MEDIUM** / 5.6 |
| **Reproduction** | statically_confirmed |
| **Patch Status** | VERIFIED_SECURE (transitively via constituents) |

**Constituent findings:**

- `abd5f395-a5e3-42b9-a8fa-94a94fa63cee` — requirements.txt pins pydantic-email-validation==0.2.0 which does not exist on PyPI (status: VALID, patch: VERIFIED_SECURE)
- `2b227e4c-7da7-40ec-bda7-8eb204014999` — Backend fails to boot: self-import of PlanDB in plan.py causes ImportError (status: VALID, patch: VERIFIED_SECURE)
- `70221333-ed67-427d-bbc5-1446b95d0c84` — Contact router imports undefined ContactRequest/ContactResponse classes (status: VALID, patch: VERIFIED_SECURE)

**Description.** Step 1 (Finding: requirements.txt pins nonexistent package): a fresh deployment runs `pip install -r backend/requirements.txt`; pydantic-email-validation==0.2.0 does not exist on PyPI (registry response: 'from versions: none'), so dependency resolution FAILS and the environment is never created. Step 2 (Finding: plan.py self-import): even if the install issue is worked around, importing app.api.routes.plans raises ImportError ('cannot import name PlanDB from partially initialized module', plan.py:6) and the FastAPI app cannot boot. Step 3 (Finding: contact router undefined classes): even after...

**Impact.** Complete loss of service availability: no clean environment can install the dependencies, no code state can boot the backend, and all API endpoints plus the marketing site's backend features are dead.

**Risk rationale.** A chain that proves NO clean environment can ever run this backend - the single most embarrassing possible state for a production repo.

**Remediation.** Break the chain at every link: (1) replace pydantic-email-validation with email-validator and verify a clean venv install; (2) remove the self-import at plan.py:6; (3) define ContactRequest/ContactResponse or remove the contact router stub; (4) add a CI startup smoke test (uvicorn boot + import app.main).

---

### Exploit Chain: Unauthenticated error-oracle (CWE-209) + unauthenticated PII writes (CWE-287) - recon to DB abuse

| Field | Value |
|---|---|
| **Finding ID** | `6d64241e-19df-44df-9027-c4c38d2cf95d` |
| **Priority / Score** | **MEDIUM** / 4.8 |
| **Reproduction** | statically_confirmed |
| **Patch Status** | VERIFIED_SECURE (transitively via constituents) |

**Constituent findings:**

- `0cbda088-0dd1-4c78-a902-efa7e271672c` — CWE-287: unauthenticated, unlimited lead/contact writes (no auth, no rate limiting, no CSRF) (status: VALID, patch: VERIFIED_SECURE)
- `d072da85-80ee-4e8b-a44a-efef216d3b9d` — CWE-209: raw exception text leaked to unauthenticated clients via detail=str(e) (status: VALID, patch: VERIFIED_SECURE)
- `64f00b37-7afa-4eb7-a214-ce0ff0c010bb` — DEBUG defaults to True: Swagger UI (/docs) exposed in production by default (status: VALID, patch: VERIFIED_SECURE)

**Description.** Step 1 (Finding: unauthenticated writes): attacker sends arbitrary JSON to POST /api/leads/create with no auth, rate limit or CSRF (lead.py:10). Step 2 (Finding: raw exception text): by triggering server-side exceptions on that same endpoint (verified: an unauthenticated request returned HTTP 400 with the full SQLAlchemy error including the INSERT statement, bound parameters, and internal module paths), the attacker harvests internal intel: SQL dialect details, parameter values, code layout, DB state. Step 3: with the error oracle confirmed and no throttling, the attacker floods the endpoint w...

**Impact.** Compounded impact: internal information disclosure (SQL, parameters, paths) + unbounded PII database pollution and disk growth, all unauthenticated - higher than either finding alone (recon data feeds the abuse, and the abuse feeds more recon through the error oracle).

**Risk rationale.** Anonymous error-oracle feeding anonymous PII-flooding on a company's own intake form; reporters love 'your site leaks its own database internals to anyone'.

**Remediation.** Fix at least one link: add authentication/rate limiting to the lead endpoint, replace detail=str(e) with a generic error mapping (log details server-side), and default DEBUG=False to hide /docs.

---

## Resolved This Campaign

All 7 reproduced findings received patches that were applied to shadow trees and verified (baseline re-triggered, patched attack blocked, integrity/benign controls passed). The patches are **not applied to the live repository** — they await human review.

| Finding | Patch | Verified result |
|---|---|---|
| plan.py self-import | remove self-import line | BLOCKED; imports OK |
| contact undefined classes | define models in models/contact.py + redirect import | BLOCKED; imports OK |
| CWE-209 raw error leak | generic error message in 3 routes | BLOCKED; benign 200 |
| CWE-287 unauthenticated writes | per-IP rate limit (10/min, 429) | THROTTLED; benign 200 |
| CWE-942 CORS | credentials off; methods/headers restricted | TIGHTENED; variants blocked |
| DEBUG default True | default False; /docs gated | HIDDEN; opt-in works |
| Nonexistent pinned package | email-validator==2.0.0 | RESOLVES; EmailStr OK |

## Appendix: Low Priority Findings

#### CWE-942: CORS allow_credentials=True with wildcard methods/headers and hardcoded origins

| Field | Value |
|---|---|
| **Finding ID** | `a801cc40-0482-4510-86f7-8410a284db8c` |
| **CWE** | CWE-942 |
| **Final Risk Score** | 2.8 / 10 |
| **Priority** | **LOW** |
| **Inferred Exposure** | EXPOSED |
| **Attacker Position** | EXTERNAL |
| **Privileges Required** | NONE |
| **User Interaction** | REQUIRED |
| **Reproduction** | reproduced |
| **Patch Status** | VERIFIED_SECURE |
| **Discovery Snapshot** | (legacy — not recorded) |

**Description.** main.py:13-19 configures CORSMiddleware with allow_credentials=True, allow_methods=['*'], allow_headers=['*'] and allow_origins=settings.CORS_ORIGINS (localhost:3000, localhost:5173, https://dfcont.com.br, hardcoded in config.py:17-21). With credentials enabled, any origin that is or becomes allowlisted can read credentialed responses; the methods/headers wildcards maximize what a compromised/evil page on an allowlisted origin can request. The localhost origins are dev-only and should not exist in a production allowlist; the real deployed origin list is unverified. CORS is defense-in-depth onl...

**Impact.** Credential-bearing cross-origin requests permitted broadly; if an attacker ever controls content on an allowlisted origin (or the allowlist drifts), credentialed data and state-changing calls become reachable from the browser.

**Validation reasoning.** main.py:13-19 and config.py:17-21 verified: allow_credentials=True, allow_methods/headers '*', 3 hardcoded origins incl. two localhost entries. Factual misconfiguration posture; low severity because no attacker-controlled origin is currently allowlisted and there are no cookies in use - defense-in-depth gap, keep as LOW hardening item.

**Production viability.** CORS config with credentials+wildcards is the shipped production configuration; hardening gap present in release builds. Low severity but viable.

**Reproduction evidence.**

```
PoC script : D:\New Page DFCont\workspace\reproducers\poc_F3_F6.py
Run command: python poc_F3_F6.py (TestClient preflight)
Evidence base: UNPINNED-MODE-OFF
```

Excerpt of reproduction output (redacted):

```
OPTIONS preflight from Origin https://dfcont.com.br returned: access-control-allow-origin: https://dfcont.com.br, access-control-allow-credentials: true, access-control-allow-methods: DELETE, GET, HEAD, OPTIONS, PATCH, POST, PUT (wildcard expansion), access-control-allow-headers: content-type.
```

**Risk rationale.** Impact 2, likelihood 2 (exposure multiplier applied); Low public outrage: abuse requires an allowlisted origin to host attacker content; no active incident vector today.

**Executive summary.** CORS credentials + wildcard methods/headers for 3 hardcoded origins (incl. localhost dev origins). Exploitation needs attacker-controlled content on an allowlisted origin (uncommon). Impact 2, likelihood 2, user-interaction multiplier 0.7 -> 2.8 LOW.

**Remediation.** Restrict methods/headers to the concrete verbs and headers used; remove localhost origins from the production allowlist; document the exact production origin(s); if credentials are not actually needed, set allow_credentials=False.

**Verified patch.** CORS credentials off, methods GET/POST/OPTIONS, headers Content-Type; evil origin/DELETE/authorization blocked — Apply against: (not recorded)

---
